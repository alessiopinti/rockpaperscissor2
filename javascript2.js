function getComputerChoice(){
  let playerSelection = prompt("rock, paper or scissors?");
  let selection = ["rock", "paper", "scissors"];
  let item = selection[Math.floor(Math.random()*selection.length)];
}
